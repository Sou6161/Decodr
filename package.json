{
  "name": "decodr",
  "version": "0.1.0",
  "private": true,
  "description": "Decodr — Understand any React codebase in minutes. Intelligent React + TypeScript codebase analysis platform.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:api dev:web",
    "dev:api": "npm run dev --workspace @decodr/api",
    "dev:web": "npm run dev --workspace @decodr/web",
    "build": "npm run build --workspace @decodr/types && npm run build --workspace @decodr/api && npm run build --workspace @decodr/web",
    "build:types": "npm run build --workspace @decodr/types",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "db:generate": "npm run db:generate --workspace @decodr/api",
    "db:migrate": "npm run db:migrate --workspace @decodr/api",
    "db:studio": "npm run db:studio --workspace @decodr/api"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.7.2"
  }
}
